export const CustomInfo = ({
  title,
  children,
  image,
  imageSize = "50px",
  border = "12px",
}) => {
  return (
    <div className="custom-styled-info">
      <Info>
        <div style={{ display: "flex", alignItems: "flex-start", gap: "16px" }}>
          {image && (
            <img
              src={image}
              style={{ width: imageSize, height: "auto", marginTop: "0" }}
            />
          )}
          <div>
            {title && (
              <div style={{ fontWeight: "bold", marginBottom: "8px" }}>
                {title}
              </div>
            )}
            <div>{children}</div>
          </div>
        </div>
      </Info>
    </div>
  );
};

export default CustomInfo;
